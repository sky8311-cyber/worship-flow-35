

# AtelierProblem 하단 텍스트 삭제

구분선과 "주일엔 뜨겁게 예배하고, 월요일엔 다시 각자의 일상으로 흩어집니다." 부분을 삭제합니다.

## 변경 (`AtelierProblem.tsx`)

Lines 19-24 (구분선 `<div>` + 하단 `<p>`) 삭제. 나머지 두 줄("우리는 예배를 준비하지만" / "삶과는 연결되지 않습니다")만 유지.

