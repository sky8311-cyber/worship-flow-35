

# 모바일 빌딩 높이 80vh로 변경

`src/components/worship-studio/StudioSidePanel.tsx` 695번 줄에서 `h-[50vh]` → `h-[80vh]`로 변경합니다.

한 줄 수정입니다.

**주의**: 80vh는 시트 전체 높이(85vh)에 거의 근접하므로, 상가/도로가 시트 밖으로 밀려 잘릴 수 있습니다. 결과를 확인 후 조정이 필요할 수 있습니다.

